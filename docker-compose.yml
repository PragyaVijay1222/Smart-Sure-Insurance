version: '3.9'

services:
  # -------------------- Databases & Messaging --------------------
  mysql:
    image: mysql:8
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: Arcana
      MYSQL_DATABASE: database
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # -------------------- Config Server --------------------
  config-server-smart-sure:
    image: pragyavijay/config-server:latest
    container_name: config-server-smart-sure
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8888:8888"
    depends_on:
      mysql:
        condition: service_healthy
    restart: always

  # -------------------- Eureka / Service Registry --------------------
  service-registry-smart-sure:
    image: pragyavijay/service-registry:latest
    container_name: service-registry-smart-sure
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8761:8761"
    depends_on:
      config-server-smart-sure:
        condition: service_started
    restart: always

  # -------------------- API Gateway --------------------
  api-gateway-smartsure:
    image: pragyavijay/api-gateway:latest
    container_name: api-gateway-smartsure
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8080:8080"
    depends_on:
      service-registry-smart-sure:
        condition: service_started
    restart: always

  # -------------------- Microservices --------------------
  auth-service:
    image: pragyavijay/auth-service:latest
    container_name: auth-service
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8081:8081"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      service-registry-smart-sure:
        condition: service_started
    restart: always

  policy-service:
    image: pragyavijay/policy-service:latest
    container_name: policy-service
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8082:8082"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      service-registry-smart-sure:
        condition: service_started
    restart: always

  payment-service:
    image: pragyavijay/payment-service:latest
    container_name: payment-service
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8085:8085"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      service-registry-smart-sure:
        condition: service_started
    restart: always

  claim-service:
    image: pragyavijay/claim-service:latest
    container_name: claim-service
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8083:8083"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      service-registry-smart-sure:
        condition: service_started
    restart: always

  admin-service:
    image: pragyavijay/admin-service:latest
    container_name: admin-service
    environment:
      - JAVA_OPTS=-Xmx256m -Xms256m
    ports:
      - "8084:8084"
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      service-registry-smart-sure:
        condition: service_started
    restart: always

  # -------------------- Zipkin --------------------
#  zipkin:
#    image: openzipkin/zipkin:2.27
#    container_name: zipkin
#    ports:
#      - "9411:9411"
#    restart: always

  # -------------------- Prometheus --------------------
#  prometheus:
#    image: prom/prometheus:latest
#    container_name: prometheus
#    volumes:
#      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
#    ports:
#      - "9090:9090"
#    restart: always

  # -------------------- Grafana --------------------
#  grafana:
#    image: grafana/grafana:10.2.0
#    container_name: grafana
#    ports:
#      - "3000:3000"
#    environment:
#      - GF_SECURITY_ADMIN_USER=admin
#      - GF_SECURITY_ADMIN_PASSWORD=admin
#    depends_on:
#      prometheus:
#        condition: service_started
#    restart: always

# -------------------- Volumes --------------------
volumes:
  mysql-data: