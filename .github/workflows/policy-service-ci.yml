name: CI - PolicyService

on:
  push:
    paths:
      - 'PolicyService/**'
      - '.github/workflows/policy-service-ci.yml'
  pull_request:
    paths:
      - 'PolicyService/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: |
          cd PolicyService
          mvn clean package -DskipTests

      - name: Run Tests
        run: |
          cd PolicyService
          mvn test
